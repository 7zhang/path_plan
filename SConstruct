path_plan = Program('bin/path_plan', ['src/common/axis.cpp', 'src/common/exp_criteria.cpp', 'src/common/function.cpp', 'src/common/geometric.cpp', 'src/kunshan/kunshan_robot.cpp', 'src/common/det6x6.cpp','src/common/load_seam.cpp', 'src/common/robotdata.cpp', 'src/common/robotkinematic.cpp', 'src/common/sine_criteria.cpp','src/common/teach_point.cpp', 'test/test.cpp', 'src/kunshan/Transform.cpp'],LIBS = ['cd', 'boost_program_options', 'pthread', 'boost_system', 'boost_thread'],CCFLAGS = '-Iinclude -O2 -std=c++11', LINKFLAGS =  '-Lbin', CPPPATH='include')

server = Program('bin/server', ['src/plan_server.cpp', 'src/common/geometric.cpp', 'src/common/axis.cpp', 'src/common/exp_criteria.cpp', 'src/common/function.cpp', 'src/common/det6x6.cpp', 'src/kuka/kr5arc_robot.cpp', 'src/common/load_seam.cpp', 'src/common/robotdata.cpp', 'src/common/robotkinematic.cpp', 'src/common/sine_criteria.cpp','src/common/teach_point.cpp', 'src/kuka/kr5arc.cpp'], LIBS = ['cd', 'jsoncpp', 'jsonrpccpp-common', 'jsonrpccpp-server', 'microhttpd', 'boost_program_options', 'pthread', 'boost_system', 'boost_thread'], CCFLAGS = '-Iinclude -O2 -std=c++11', LINKFLAGS =  '-Lbin', CPPPATH='include')

client = Program('bin/client', ['src/client.cpp', 'src/common/load_seam.cpp', 'src/common/geometric.cpp'], LIBS = ['cd', 'jsoncpp', 'jsonrpccpp-common', 'jsonrpccpp-client', 'boost_program_options', 'pthread', 'boost_system', 'boost_thread'],  CCFLAGS = '-Iinclude -O2 -std=c++11', LINKFLAGS =  '-Lbin', CPPPATH='include')

kr5 = Program('bin/kr5', ['src/common/axis.cpp', 'src/common/exp_criteria.cpp', 'src/common/det6x6.cpp', 'src/common/function.cpp', 'src/common/geometric.cpp', 'src/common/load_seam.cpp', 'src/common/robotdata.cpp', 'src/common/robotkinematic.cpp', 'src/common/sine_criteria.cpp', 'src/common/teach_point.cpp', 'src/kuka/kr5arc.cpp', 'src/kuka/kr5arc_robot.cpp','test/test_kr5.cpp'], LIBS = ['cd', 'boost_program_options', 'pthread', 'boost_system', 'boost_thread'], CCFLAGS = '-Iinclude -O2 -std=c++11', LINKFLAGS =  '-Lbin', CPPPATH='include')

server1 = Program('bin/server1', ['src/plan_server.cpp', 'src/common/geometric.cpp', 'src/common/axis.cpp', 'src/common/exp_criteria.cpp', 'src/common/function.cpp', 'src/common/det6x6.cpp', 'src/kuka1/kr5arc_robot.cpp', 'src/common/load_seam.cpp', 'src/common/robotdata.cpp', 'src/common/robotkinematic.cpp', 'src/common/sine_criteria.cpp','src/common/teach_point.cpp', 'src/kuka1/kr5arc.cpp', 'src/kuka1/positioner.cpp', 'src/kuka1/weld_point.cpp'], LIBS = ['cd', 'jsoncpp', 'jsonrpccpp-common', 'jsonrpccpp-server', 'microhttpd', 'boost_program_options', 'pthread', 'boost_system', 'boost_thread'], CCFLAGS = '-Iinclude -O2 -std=c++11', LINKFLAGS =  '-Lbin', CPPPATH='include')